apiVersion: kueue.x-k8s.io/v1beta1
kind: WorkloadPriorityClass
metadata:
  name: pipeline-low-priority
value: 100
description: "Low priority — these workloads can be preempted by higher priorities"
---
apiVersion: kueue.x-k8s.io/v1beta1
kind: WorkloadPriorityClass
metadata:
  name: pipeline-default-priority
value: 1000
description: "Default priority for pipeline workloads"
---
apiVersion: kueue.x-k8s.io/v1beta1
kind: WorkloadPriorityClass
metadata:
  name: pipeline-high-priority
value: 10000
description: "High priority — can preempt lower priority workloads in the same queue"
